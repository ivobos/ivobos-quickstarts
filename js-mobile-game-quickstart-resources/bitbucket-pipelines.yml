image: ivobos/js-mobile-game-quickstart-ci-image:0.0.2

pipelines:
  tags:
    v*:
      - step:
          caches:
            - node
          script:
            - npm install
            - npm run android

options:
  docker: true
